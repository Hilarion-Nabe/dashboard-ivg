# render.yaml — Deployment configuration for Render.com
# Push this to your GitHub repo root alongside app.py

services:
  - type: web
    name: ivg-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
